name: Unit tests

on:
  push:
    branches:
      - main
      - 4.x
  pull_request:
    types: [opened, synchronize, reopened]
    branches-ignore: ['l10n_main*']
  release:
    types: [created]

  workflow_run:
    workflows: ['Compress images']
    types: [completed]

env:
  default-php-version: '8.4'
  node-version: 22

concurrency:
  group: Unit tests ${{ github.ref }}
  cancel-in-progress: true

jobs:
  #############
  # Run tests
  #############
  tests:
    runs-on: ubuntu-latest
    name: Testsuite ${{ matrix.testsuite }} (${{ matrix.connection }})

    strategy:
      fail-fast: false
      matrix:
        connection: [mysql]
        testsuite: [Api, Feature, Commands-Other, Commands-Scheduling, Unit-Models, Unit-Services]
        # exclude:
        #   - php-version: '8.1'
        #     testsuite: Feature

    steps:
      - name: Checkout sources
        uses: actions/checkout@v5

      - name: Setup PHP ${{ env.default-php-version }}
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ env.default-php-version }}
          extensions: redis, ${{ matrix.connection }}
          coverage: xdebug
      - name: Check PHP Version
        run: php -v
      - name: Check Composer Version
        run: composer -V
      - name: Check PHP Extensions
        run: php -m

    # Composer
      - name: Validate composer.json and composer.lock
        run: composer validate

      - name: Get Composer Cache Directory
        id: composer-cache
        run: echo "dir=$(composer config cache-files-dir)" >> $GITHUB_OUTPUT
      - name: Cache composer files
        uses: actions/cache@v4
        with:
          path: ${{ steps.composer-cache.outputs.dir }}
          key: ${{ runner.os }}-composer-${{ env.default-php-version }}-${{ hashFiles('**/composer.lock') }}
          restore-keys: |
            ${{ runner.os }}-composer-${{ env.default-php-version }}-${{ hashFiles('**/composer.lock') }}
            ${{ runner.os }}-composer-${{ env.default-php-version }}
            ${{ runner.os }}-composer-

      - name: Install composer dependencies
        run: composer install --no-progress --no-interaction --prefer-dist --optimize-autoloader $ext

    # Prepare
      - name: Prepare environment
        run: |
          cp scripts/ci/.env.${{ matrix.connection }} .env
          echo 'v2.17.0' > config/.version
          touch config/.release config/.commit
          mkdir -p public/js public/css results/coverage
          {\
            echo "{"; \
            for f in app.js manifest.js vendor.js app-ltr.css app-rtl.css stripe.js stripe.css; do \
              [[ $first == 1 ]] && echo -n "," || first=1; \
              k=${f##*.}/$f; \
              echo "\"/$k\": \"/$k\""; \
              echo '' > public/$k; \
            done; \
            echo "}"; \
          } | tee public/mix-manifest.json

      - name: Generate key
        run: php artisan key:generate

      - name: Start mysql
        if: matrix.connection == 'mysql'
        run: sudo systemctl start mysql.service
      - name: Create database

        if: matrix.connection == 'mysql'
        run: mysql --protocol=tcp -u root -proot -e "CREATE DATABASE IF NOT EXISTS monica CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"

      - name: Prepare database
        if: matrix.connection == 'mysql'
        run: mysql --protocol=tcp -u root -proot monica < scripts/database.test.sql

      - name: Run migrations
        run: php artisan migrate --no-interaction -vvv

      - name: Run seeds
        run: php artisan db:seed --no-interaction -vvv

      - name: Create passport keys
        run: php artisan passport:keys --no-interaction -vvv

      - name: Cache route
        run: php artisan route:cache

    # Test
      - name: Run Unit test suite
        run: |
          vendor/bin/phpunit -c phpunit.xml \
            --testsuite ${{ matrix.testsuite }} \
            --log-junit ./results/junit/results${{ matrix.testsuite }}.xml \
            --coverage-clover ./results/coverage/coverage${{ matrix.testsuite }}.xml \
            --coverage-php ./results/cov/${{ matrix.testsuite }}.cov
        env:
          DB_CONNECTION: ${{ matrix.connection }}
          STRIPE_SECRET: ${{ secrets.STRIPE_SECRET }}
          XDEBUG_MODE: coverage

      - name: Fix results files
        run: sed -i -e "s%$GITHUB_WORKSPACE/%%g" **/*.xml
        working-directory: results

      - name: Store results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: results-${{ matrix.testsuite }}
          path: results


  ###################
  # Run tests browser
  ###################
  tests_browser:
    runs-on: ubuntu-latest
    name: Tests browser (${{ matrix.connection }})

    strategy:
      fail-fast: false
      matrix:
        connection: [mysql]

    steps:
      - name: Checkout sources
        uses: actions/checkout@v5

      - name: Setup PHP ${{ env.default-php-version }}
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ env.default-php-version }}
          extensions: redis, ${{ matrix.connection }}
          coverage: xdebug
      - name: Check PHP Version
        run: php -v
      - name: Check Composer Version
        run: composer -V
      - name: Check PHP Extensions
        run: php -m

    # Composer
      - name: Validate composer.json and composer.lock
        run: composer validate

      - name: Get Composer Cache Directory
        id: composer-cache
        run: echo "dir=$(composer config cache-files-dir)" >> $GITHUB_OUTPUT
      - name: Cache composer files
        uses: actions/cache@v4
        with:
          path: ${{ steps.composer-cache.outputs.dir }}
          key: ${{ runner.os }}-composer-${{ env.default-php-version }}-${{ hashFiles('**/composer.lock') }}
          restore-keys: |
            ${{ runner.os }}-composer-${{ env.default-php-version }}-${{ hashFiles('**/composer.lock') }}
            ${{ runner.os }}-composer-${{ env.default-php-version }}
            ${{ runner.os }}-composer-

      - name: Install composer dependencies
        run: composer install --no-progress --no-interaction --prefer-dist --optimize-autoloader $ext

    # Yarn
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.node-version }}
          cache: yarn

      - name: Install yarn dependencies
        run: yarn inst
      - name: Build assets
        run: yarn run production

    # Prepare
      - name: Prepare environment
        run: |
          cp scripts/ci/.env.${{ matrix.connection }} .env
          echo 'v2.17.0' > config/.version
          touch config/.release config/.commit
          mkdir -p results/coverage results/cov results/console
          chmod -R 777 storage bootstrap/cache

      - name: Generate key
        run: php artisan key:generate

      - name: Start mysql
        if: matrix.connection == 'mysql'
        run: sudo systemctl start mysql.service

      - name: Create database
        if: matrix.connection == 'mysql'
        run: mysql --protocol=tcp -u root -proot -e "CREATE DATABASE IF NOT EXISTS monica CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;"

      - name: Prepare database
        if: matrix.connection == 'mysql'
        run: mysql --protocol=tcp -u root -proot monica < scripts/database.test.sql

      - name: Run migrations
        run: php artisan migrate --no-interaction -vvv

      - name: Run seeds
        run: php artisan db:seed --no-interaction -vvv

      - name: Create passport keys
        run: php artisan passport:keys --no-interaction -vvv

      - name: Cache route
        run: php artisan route:cache

      - name: Test coverage page
        run: REQUEST_URI=/ php scripts/tests/server-cc.php

      - uses: browser-actions/setup-chrome@v2
        id: setup-chrome
        with:
          install-chromedriver: true

    # Test
      - name: Upgrade Chrome Driver
        run: php artisan dusk:chrome-driver --detect

      - name: Run http server
        run: |
          php -S localhost:8000 -t public scripts/tests/server-cc.php &
          php -S localhost:8001 -t public scripts/tests/server-cc.php &

      - name: Run browser tests
        run: php artisan dusk --log-junit results/junit/results0.xml
        env:
          APP_URL: http://localhost:8000
          CHROME_PATH: ${{ steps.setup-chrome.outputs.chrome-path }}

      - name: Fix results files
        run: sed -i -e "s%$GITHUB_WORKSPACE/%%g" **/*.xml
        working-directory: results

      - name: Store results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: results
          path: results


  ###########################
  # Reporting to sonarcloud
  ###########################
  reporting:
    needs: [tests, tests_browser]
    runs-on: ubuntu-latest
    name: Sonarcloud
    if: ${{ ! startsWith(github.ref, 'dependabot/') }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0  # Shallow clones should be disabled for a better relevancy of analysis

      - name: Setup PHP ${{ env.default-php-version }}
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ env.default-php-version }}

      - name: Download results
        uses: actions/download-artifact@v5
        with:
          path: results
          merge-multiple: true

    # Composer
      - name: Validate composer.json and composer.lock
        run: composer validate

      - name: Get Composer Cache Directory
        id: composer-cache
        run: echo "dir=$(composer config cache-files-dir)" >> $GITHUB_OUTPUT
      - name: Cache composer files
        uses: actions/cache@v4
        with:
          path: ${{ steps.composer-cache.outputs.dir }}
          key: ${{ runner.os }}-composer-${{ env.default-php-version }}-${{ hashFiles('**/composer.lock') }}
          restore-keys: |
            ${{ runner.os }}-composer-${{ env.default-php-version }}-${{ hashFiles('**/composer.lock') }}
            ${{ runner.os }}-composer-${{ env.default-php-version }}
            ${{ runner.os }}-composer-

      - name: Install composer dependencies
        run: composer install --no-progress --no-interaction --prefer-dist --optimize-autoloader $ext

      - name: Fix coverage
        run: |
          vendor/bin/phpcov merge --clover=results/coverage/coverageBrowser.xml results/cov/
          rm -rf results/cov

      - name: Merge junit files
        run: |
          yarn global add junit-report-merger
          $(yarn global bin)/jrm ./results/results.xml ./junit/*.xml

      - name: Set version parameter
        id: version
        run: |
          version=$(git tag --points-at HEAD)
          test -z "$version" && version="master"
          echo "value=$version" >> $GITHUB_OUTPUT

      - name: Set coverage list
        id: coverage
        run: |
          SONAR_COVERAGE=$(ls -m --format=comma results/coverage/coverage*.xml | sed -e ':a;N;$!ba;s/\n//g; s/ //g;')
          echo "list=$SONAR_COVERAGE" >> $GITHUB_OUTPUT

      - name: SonarCloud Scan
        if: env.SONAR_TOKEN != ''
        uses: SonarSource/sonarqube-scan-action@v5
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        with:
          args: |
            -Dsonar.projectVersion=${{ steps.version.outputs.value }}
            -Dsonar.php.tests.reportPath=./results/results.xml
            -Dsonar.php.coverage.reportPaths=${{ steps.coverage.outputs.list }}
